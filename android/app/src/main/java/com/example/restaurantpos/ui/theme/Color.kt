package com.example.restaurantpos.ui.theme

import androidx.compose.ui.graphics.Color

// === Premium Dark Theme Colors (matching Web CSS variables) ===

// Brand
val BrandAmberGold = Color(0xFFFFB300)
val BrandGoldLight = Color(0xFFFFDF00)
val BrandGoldDark = Color(0xFFD4AF37)

// Backgrounds
val DarkBase = Color(0xFF050505)
val DarkSurface = Color(0xFF141414)
val DarkSurfaceElevated = Color(0xFF1A1A1A)
val DarkSurfaceCard = Color(0xFF1E1E1E)
val DarkSurfaceHover = Color(0xFF252525)

// Text
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFA0A0A0)
val TextMuted = Color(0xFF7F8C8D)

// Status Colors
val StatusPending = Color(0xFFE74C3C)    // Red — needs attention
val StatusCooking = Color(0xFF3498DB)    // Blue — in progress
val StatusReady = Color(0xFF27AE60)      // Green — ready to serve
val StatusCompleted = Color(0xFF95A5A6)  // Gray — done
val StatusCancelled = Color(0xFF7F8C8D)  // Muted gray

// Order Type Colors
val OrderDineIn = Color(0xFF27AE60)      // Green
val OrderTakeaway = Color(0xFF3498DB)    // Blue
val OrderDelivery = Color(0xFFE67E22)    // Orange

// Accents
val AccentWarning = Color(0xFFE67E22)    // Orange — notes/warnings
val AccentDanger = Color(0xFFE74C3C)     // Red — reject/cancel
val AccentSuccess = Color(0xFF27AE60)    // Green — complete/accept

// Surface Borders
val BorderSubtle = Color(0x14FFFFFF)     // 8% white
val BorderHighlight = Color(0x4DD4AF37)  // 30% gold
